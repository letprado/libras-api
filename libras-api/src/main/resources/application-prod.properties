# Perfil usado no deploy (Docker Compose local / Railway / VPS)
# PORT = injetado pelo Railway; SERVER_PORT = docker-compose local
server.port=${PORT:${SERVER_PORT:8080}}

spring.rabbitmq.host=${SPRING_RABBITMQ_HOST:localhost}
spring.rabbitmq.port=${SPRING_RABBITMQ_PORT:5672}
spring.rabbitmq.username=${SPRING_RABBITMQ_USERNAME:guest}
spring.rabbitmq.password=${SPRING_RABBITMQ_PASSWORD:guest}

feign.session-service.url=${FEIGN_SESSION_SERVICE_URL:http://127.0.0.1:${PORT:${SERVER_PORT:8080}}}
